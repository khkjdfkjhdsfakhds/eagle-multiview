# MultiView Review TXT 后台保存

此配套服务仅用于本轮 Eagle MultiView Review，使用 Eagle 官方 `eagle.item.getById()` 与 `Item.replaceFile()`，不打开或选中素材、不主动显示窗口。不是通用网络执行工具，也不会扫描或修改未提交为保存目标的素材。

## 启用

1. 在 Eagle 插件管理界面中，通过当前版本提供的本地开发插件加载入口，选择本文件所在目录（其中包含 `manifest.json`）。需由 Eagle 确认加载并启用后台服务。
2. 启动 Eagle MultiView Review。服务通过 Review 自身数据目录中的私有连接文件连接本机回环端口；不要求输入密钥，不监听局域网。
3. 只在专用测试库中首次验证 TXT 输入、保存与前台保持，确认后再用于用户明确编辑的素材。

构建版随 App 附带完整目录，位于应用包 `Contents/Resources/Eagle TXT Save Plugin`。不要只复制一个 JS 文件或手工修改 Eagle 资料库。

## 行为与边界

- 只接受指定库、指定 TXT ID、匹配预期内容版本且位于 Review 暂存目录的候选文件。单份最大 5 MB。
- 不以 SDK 的 `undefined` 返回值判失败；写后比较正文哈希，缩略图延迟单独反馈。
- 结果不确定时先做只读核验；不会重新执行原保存请求。真实未完成的替换保持门禁，避免后到写覆盖前到内容。
- 零字节正文按当前 Eagle SDK 限制保留为草稿，不偷偷补字符或改成直接写库。
- 插件依赖 Eagle 运行；退出或停用后 Review 仍保留草稿。无需给服务配置任意文件路径或网络地址。
- 草稿、暂存、版本备份、鉴权文件均位于本轮 Review 的独立数据目录；正式版和历史 Review 的用户状态不复用。

本说明描述实现与启用要求，不代替交付报告中的真实 Eagle/焦点验收记录。合并为未来正式版本时，须同时调整插件与客户端的 profile 身份，不直接沿用本轮 Review 配对信息。
